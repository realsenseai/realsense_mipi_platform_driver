From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: zhou <shengyong.zhou@realsenseai.com>
Date: Mon, 27 Jul 2026 22:45:00 +0800
Subject: [PATCH] media: tegra: receive D5x Y12I over RAW16

D5x stores calibration IR as little-endian L/R 16-bit containers and
transmits both containers unchanged using CSI RAW16.

Add a private mbus tuple that matches RAW16 but uses T_R8 byte DMA. This
preserves the source byte order without a frame callback while exposing
the standard V4L2 Y16I surface. Scope the transport-width and DT override
handling to that tuple so generic RAW16 and existing D4xx formats remain
unchanged.

Signed-off-by: zhou <shengyong.zhou@realsenseai.com>
---
diff --git a/drivers/media/platform/tegra/camera/vi/vi5_fops.c b/drivers/media/platform/tegra/camera/vi/vi5_fops.c
index 8889cb2e..40c8a0e0 100644
--- a/drivers/media/platform/tegra/camera/vi/vi5_fops.c
+++ b/drivers/media/platform/tegra/camera/vi/vi5_fops.c
@@ -396,6 +396,8 @@ static void vi5_setup_surface(struct tegra_channel *chan,
 	bool flat_nv12 = chan->fmtinfo->fourcc == V4L2_PIX_FMT_NV12;
 	bool d5x_sgrbg10p =
 		chan->fmtinfo->fourcc == V4L2_PIX_FMT_RS_SGRBG10P;
+	bool d5x_y12i_raw16 =
+		chan->fmtinfo->code == MEDIA_BUS_FMT_RS_Y12I_RAW16_2X16;
 	u32 nvcsi_stream = chan->port[vi_port];
 	struct capture_descriptor_memoryinfo *desc_memoryinfo =
 		&chan->tegra_vi_channel[vi_port]->
@@ -414,6 +416,9 @@ static void vi5_setup_surface(struct tegra_channel *chan,
 	else if (d5x_sgrbg10p)
 		/* RAW8 parser width is the byte-identical HKR DDR stride. */
 		width = bpl;
+	else if (d5x_y12i_raw16)
+		/* RAW16 contributes two transport samples per logical L/R pair. */
+		width = bpl / 2;
 
 	memcpy(desc, &capture_template, sizeof(capture_template));
 	memset(desc_memoryinfo, 0, sizeof(*desc_memoryinfo));
@@ -432,6 +437,13 @@ static void vi5_setup_surface(struct tegra_channel *chan,
 		 */
 		desc->ch_cfg.dt_enable = 1;
 		desc->ch_cfg.dt_override = TEGRA_IMAGE_DT_RAW8;
+	} else if (d5x_y12i_raw16) {
+		/*
+		 * Match CSI RAW16, then use T_R8 byte DMA so L/R container
+		 * endianness reaches the standard V4L2 Y16I surface unchanged.
+		 */
+		desc->ch_cfg.dt_enable = 1;
+		desc->ch_cfg.dt_override = TEGRA_IMAGE_DT_RAW8;
 	}
 	desc->ch_cfg.pixfmt_enable = 1;
 	desc->ch_cfg.pixfmt.format = format;
diff --git a/drivers/media/platform/tegra/camera/vi/vi5_formats.h b/drivers/media/platform/tegra/camera/vi/vi5_formats.h
index 0d200227..11769852 100644
--- a/drivers/media/platform/tegra/camera/vi/vi5_formats.h
+++ b/drivers/media/platform/tegra/camera/vi/vi5_formats.h
@@ -90,6 +90,10 @@ enum tegra_image_format {
 #define MEDIA_BUS_FMT_RS_SGRBG10P_RAW16_1X16 0x5004
 #endif
 
+#ifndef MEDIA_BUS_FMT_RS_Y12I_RAW16_2X16
+#define MEDIA_BUS_FMT_RS_Y12I_RAW16_2X16 0x5005
+#endif
+
 #ifndef MEDIA_BUS_FMT_RS_SGRBG10P_1X10
 #define MEDIA_BUS_FMT_RS_SGRBG10P_1X10 0x5002
 #endif
@@ -102,6 +106,10 @@ enum tegra_image_format {
 #define V4L2_PIX_FMT_SGRBG10P v4l2_fourcc('p', 'g', 'A', 'A')
 #endif
 
+#ifndef V4L2_PIX_FMT_Y16I
+#define V4L2_PIX_FMT_Y16I v4l2_fourcc('Y', '1', '6', 'I')
+#endif
+
 static const struct tegra_video_format vi5_video_formats[] = {
 	/* RAW 6: TODO */
 
@@ -211,6 +219,13 @@ static const struct tegra_video_format vi5_video_formats[] = {
 	TEGRA_VIDEO_FORMAT(RAW10, 10, RS_SGRBG10P_1X10, 32, 25, T_R8,
 				USER_1, RS_SGRBG10P, "RealSense GRBG10P CL64"),
 
+	/*
+	 * D5x native Y12I: CSI RAW16 transports little-endian L/R 16-bit
+	 * containers. T_R8 preserves the payload bytes for standard Y16I.
+	 */
+	TEGRA_VIDEO_FORMAT(RAW12, 12, RS_Y12I_RAW16_2X16, 4, 1, T_R8,
+				RAW16, Y16I, "Y12I in 16-bit L/R containers"),
+
 };
 
 #endif
